apiVersion: v1
kind: Secret
metadata:
  name: data-discovery-db-secret
  namespace: default
type: Opaque
stringData:
  # 将下面的值替换为实际的数据库密码（或使用 base64 编码后放到 data: 字段）
  db-url: "jdbc:mysql://<MySQL_HOST>:3306/Project4?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8"
  db-username: "root"
  db-password: "ibdiibdi"
