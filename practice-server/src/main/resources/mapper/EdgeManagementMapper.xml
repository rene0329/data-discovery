<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EdgeManagementMapper">
    <resultMap id="EdgeManagementResultMap" type="org.example.entity.EdgeManagement">
        <id property="edgeId" column="edge_id"/>
        <result property="sourceId" column="source_id"/>
        <result property="targetId" column="target_id"/>
        <result property="bandwidth" column="bandwidth"/>
        <result property="latency" column="latency"/>
        <result property="status" column="status"/>
        <result property="measurementTime" column="measurement_time"/>
    </resultMap>
    <sql id="logicalEdges">
        SELECT e.edge_id, LEAST(s.node_id, t.node_id) AS source_id,
               GREATEST(s.node_id, t.node_id) AS target_id,
               e.bandwidth, e.latency, e.status, e.measurement_time
        FROM logical_topology_edge e
        JOIN node_management s ON s.node_name = e.source_node_name AND s.deleted_at IS NULL
        JOIN node_management t ON t.node_name = e.target_node_name AND t.deleted_at IS NULL
    </sql>
    <select id="links" resultMap="EdgeManagementResultMap">
        <include refid="logicalEdges"/> ORDER BY e.edge_id
    </select>
    <select id="selectAllEdges" resultMap="EdgeManagementResultMap">
        <include refid="logicalEdges"/> ORDER BY e.edge_id
    </select>
    <select id="findBySourceAndTargetNode" resultMap="EdgeManagementResultMap">
        <include refid="logicalEdges"/>
        WHERE (s.node_id = #{sourceId} AND t.node_id = #{targetId})
           OR (s.node_id = #{targetId} AND t.node_id = #{sourceId})
    </select>
</mapper>
