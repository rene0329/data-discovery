# Generated fixed malicious binary threshold program.
THRESHOLD = 100
total = sum(sint.get_input_from(party) for party in range(3))
reached = total >= THRESHOLD
print_ln_to(0, 'TOPIC4_THRESHOLD threshold=100 reached=%s', reached.reveal_to(0))
